# Offline Screenshot OCR 0.8.8

0.8.8 聚焦钉图标注工具、离线脱敏、OCR 工具栏和暗色 CAD 文字识别兜底，不改变现有离线 OCR 模型、授权方式和用户数据目录。

## 主要改进

- 画笔新增实线、长虚线、短虚线、点划线和双点划线，并按工具及子类型独立记忆参数。
- 箭头扩展为六种样式；矩形与圆形分别支持边线色和填充色，填充颜色可独立调整。
- 高亮默认采用淡黄色宽幅 Mark 笔，同时保留矩形高亮。
- 马赛克支持像素与磨砂两种方式；磨砂强度达到 100% 时强化不可读性。
- 橡皮擦提供对象、笔刷和框选三种模式，并增加可撤销的清屏操作；本次返工修正笔刷局部擦除椭圆等矢量图形时的残留几何问题。
- 脱敏规则库支持固定关键词、字段标签、正则、表格列和白名单；候选列表显示来源，表格列使用 OCR 单元格精确坐标。
- OCR 入口改为固定单行按钮栏，选择、复制、图层、校审、取消和重试均可直接操作。
- F8、后台 OCR 和钉图重试统一记录输入指纹与结果摘要；暗色 CAD 在首轮明显低产出时自动尝试颜色无关增强，结果坐标映射回原图。
- “选择并拖动”状态下，标注与 OCR 图层保持可见但锁定，鼠标拖动只移动整张图片。

## 验证结果

- 162 项常规自动回归通过，3 项按环境条件跳过。
- 7 项暗色 CAD 增强定向测试通过。
- 0.8.8 alpha1 至 beta3、RC1 和正式发布门禁全部通过。
- 连续截图等价负载、工作区恢复、混合 DPI 几何、稳态资源、启动预算及安装升级数据保护门禁通过。
- 临时隔离安装、同版本覆盖和卸载通过，未改变当前正式安装现场及用户数据。

## 平台边界

- 支持 Windows 10 1809 及以上版本、Windows 11 x64。
- 本轮自动验收环境为 Windows 11 24H2；混合 DPI、负坐标副屏和断屏归位由坐标模型自动测试覆盖。
- 本轮没有新增真实 Windows 10 或真实混合 DPI 双屏硬件人工证据，因此不作超出证据范围的宣称。

## 文件校验

- 本地文件：`Offline Screenshot OCR-0.8.8-Setup.exe`
- GitHub 资产：`Offline.Screenshot.OCR-0.8.8-Setup.exe`
- 大小：`181,505,595` 字节
- SHA256：`1d79eb035e03f10bf8a6b66035a772a8db01c505ead7a5367a78e6ad33f4eb9e`

GitHub 会将 Release 资产名中的空格规范化为点号；两种名称对应同一安装包内容。
