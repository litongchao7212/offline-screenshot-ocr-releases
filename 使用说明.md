# 离线高精度截图OCR 使用说明

> 适用版本：0.8.1
> 支持系统：Windows 10 1809及以上版本、Windows 11 x64

## 1. 快速开始

1. 启动软件后，程序会在任务栏通知区域保持轻量运行。
2. 按`Ctrl+Alt+O`进入截图，拖动鼠标框选需要识别的区域。
3. 松开鼠标后按当前默认动作执行；也可在截图工具栏选择快速、精确、表格、复制或审校。
4. 普通文字复制为Unicode文本；表格同时写入TSV和HTML Table，可直接粘贴到Excel或WPS。

全局快捷键、截图默认动作、松开立即执行和截图工具栏可在“设置 → 常规与截图”中修改。

五个动作分别为：快速文字（small模型并复制）、精确文字（medium模型并打开结果页）、表格直粘、仅复制图片、打开审校。选择“等待手动选择”时，框选后不会立即执行。

## 2. 截图选区操作

- `方向键`：移动选区1像素。
- `Ctrl+方向键`：移动选区10像素。
- `Shift+方向键`：调整最后拖动的选区边缘1像素。
- `Ctrl+Shift+方向键`：调整边缘10像素。
- `Enter`：执行设置中的默认动作。
- `Esc`：取消截图。

## 3. 普通文字审校

- 点击原图中的识别框，可定位到右侧对应文字。
- 在文字区选择内容时，涉及的完整行会显示背景色；跨行选择会覆盖所有涉及行。
- 文字经过常规插入、删除或替换后，程序继续使用识别块锚点保持图文定位。
- 可使用整理、合并多行、智能分段、标点整理和查找替换等工具。
- 修改后请重新复制，避免将旧剪贴板内容粘贴到其他软件。
- 工具栏“待复核”会汇总低置信度文字、金额、日期、百分比、长编号和前导零编号；点击“定位”可回到原图识别框。
- “标记已复核”只更新复核状态，不自动修改识别文字。

## 4. 表格审校与复制

- 点击原图识别框可定位到对应单元格；选择单元格也会定位原图文字块。
- 可插入或删除行列、清空单元格、撤销、重做和恢复原表。
- 结构编辑模式支持拖动、增加或删除表格结构线。
- “复制选择”只复制当前选区；“复制表格”复制完整表格。
- 对金额、长编号、前导零、百分比和日期应在粘贴前人工复核。
- 表格识别冲突和结构异常也会进入统一“待复核”队列；点击后定位到对应单元格。

## 5. OCR进度、取消与超时

结果页会显示读取图片、加载模型、识别文字、分析版面和生成结果等阶段。识别期间可以取消；超时时间可在设置中选择。取消或超时的任务不会写入历史，也不会自动覆盖剪贴板。

## 6. 历史与页签

- 历史记录和当前结果页签均保存在本机。
- 关闭窗口后可按设置恢复上次页签；恢复后可继续编辑、复核和复制。
- 历史保存数量、页签数量和是否恢复均可在“设置 → 常规与截图”调整。
- 删除历史会同时删除对应的本机缓存图片，请确认后操作。

## 7. 在线服务与隐私

- PP-OCRv6本地识别完全离线，不上传截图或识别文字。
- 在线OCR、翻译和AI校核只在用户主动点击时联网。
- API Key使用Windows当前账户加密保存。
- AI校核默认只发送文字；上传原图需要开启权限并逐次确认。

## 8. 检查更新

- 软件每天最多自动检查一次GitHub正式版本；检查失败不影响离线OCR。
- 可在托盘菜单“检查更新”或“设置 → 关于”手动检查。
- 发现新版本后，软件只提示并打开官方GitHub Release页面，不会静默安装。
- 下载后请核对Release中的SHA256；覆盖安装会保留当前用户的设置、授权、历史和截图。

官方发布仓库：  
<https://github.com/litongchao7212/offline-screenshot-ocr-releases>

## 9. 常见问题

### 截图窗口空白或无法创建结果页

先退出托盘程序后重新启动。如仍失败，打开“设置 → 隐私与诊断 → 组件与诊断”，运行自检并保留活动日志。

### Excel/WPS中的长编号或前导零发生变化

这是表格软件自动转换格式导致的。粘贴前将目标列设为文本，或在识别结果中确认编号后再复制。

### 无法打开Markdown使用说明

程序会优先使用Windows默认Markdown应用；没有关联应用时会用记事本打开。也可以直接打开安装目录中的`使用说明.md`。

### GitHub无法连接

更新检查不会影响本地功能。可稍后重试，或通过作者发布的可信渠道获取安装包并核对SHA256。

## 10. 授权与作者

- 首次使用提供30天本机试用。
- 正式授权通过机器码和离线授权码完成，不依赖联网验证。
- 作者：李同超
- 作者公众号：工程商务杂坛

© 2026 李同超，保留所有权利。未经许可不得复制、分发或用于商业用途；第三方组件遵循各自许可证。
