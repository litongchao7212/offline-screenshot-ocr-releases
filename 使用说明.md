# 离线高精度截图OCR 使用说明

> 适用版本：0.8.8
> 支持系统：Windows 10 1809及以上版本、Windows 11 x64

## 1. 快速开始

1. 启动软件后，程序会在任务栏通知区域保持轻量运行。
2. 按`Ctrl+Alt+O`进入截图，拖动鼠标框选需要识别的区域。
3. 松开鼠标后按当前默认动作执行；也可在截图工具栏选择快速、精确、表格、复制或审校。
4. 普通文字复制为Unicode文本；表格同时写入TSV和HTML Table，可直接粘贴到Excel或WPS。
5. 默认`F1`截图并在选区边缘显示轻量后台识别进度，默认`F3`钉住最近截图，默认`F4`直接钉住剪贴板图片；均可改为其他可用快捷键。

截图默认动作、松开立即执行和截图工具栏可在“设置 → 常规与截图”中修改；贴图快捷键和行为集中在“设置 → 贴图”。

五个动作分别为：快速文字（small模型并复制）、精确文字（medium模型并打开结果页）、表格直粘、仅复制图片、打开审校。选择“等待手动选择”时，框选后不会立即执行。

## 2. 截图选区操作

- `方向键`：移动选区1像素。
- `Ctrl+方向键`：移动选区10像素。
- `Shift+方向键`：调整最后拖动的选区边缘1像素。
- `Ctrl+Shift+方向键`：调整边缘10像素。
- `Enter`：执行设置中的默认动作。
- `Esc`：取消截图。

## 3. 普通文字审校

- 点击原图中的识别框，可定位到右侧对应文字。
- 在文字区选择内容时，涉及的完整行会显示背景色；跨行选择会覆盖所有涉及行。
- 文字经过常规插入、删除或替换后，程序继续使用识别块锚点保持图文定位。
- 可使用整理、合并多行、智能分段、标点整理和查找替换等工具。
- 修改后请重新复制，避免将旧剪贴板内容粘贴到其他软件。
- 工具栏“待复核”会汇总低置信度文字、金额、日期、百分比、长编号和前导零编号；点击“定位”可回到原图识别框。
- “标记已复核”只更新复核状态，不自动修改识别文字。

## 4. 表格审校与复制

- 点击原图识别框可定位到对应单元格；选择单元格也会定位原图文字块。
- 可插入或删除行列、清空单元格、撤销、重做和恢复原表。
- 结构编辑模式支持拖动、增加或删除表格结构线。
- 拖动可选择矩形区域，Shift扩展范围，Ctrl增加非连续单元格；点击行号选择整行，点击列头选择整列，Shift/Ctrl同样适用于多列。
- “复制选区”只复制当前选区；“复制表格”复制完整表格。非连续选择中的空缺位置会保留为空单元格，防止粘贴错位。
- 复制方式菜单提供“保持文本”“智能数字”“全部按文本”。默认保持文本；智能数字会转换明确的金额、千分位和百分比；全部按文本用于禁止Excel/WPS自动转换。
- 长编号、前导零、日期和公式开头内容在所有策略下均按文本保护。对金额、百分比和日期仍应在粘贴前人工复核。
- “核算选区”会复算标有合计、总计、小计或共计的行列；差异单元格只标记和提示，不修改OCR原值。
- 表格识别冲突和结构异常也会进入统一“待复核”队列；点击后定位到对应单元格。

## 5. OCR进度、取消与超时

结果页会显示读取图片、加载模型、识别文字、分析版面和生成结果等阶段。识别期间可以取消；超时时间可在设置中选择。取消或超时的任务不会写入历史，也不会自动覆盖剪贴板。

## 6. 钉图、标注与隐私遮挡

- 在结果页“原图”工具栏点击图钉，可把当前原图打开为独立置顶窗口；历史记录也提供图钉按钮，托盘可直接钉住剪贴板图片。
- 在任意应用中按设置的全局钉图功能键（默认`F4`），会直接钉住当前剪贴板图片；该动作不要求先打开截图选区。
- 默认`F1`完成选区后仅保留贴近边缘的细虚线和淡色进度，OCR在后台进行；默认`F3`可随时钉住最近一次截图，识别完成后文字和表格层会动态附加。
- 钉图为无边框图片窗口，工具栏默认吸附在下方；空间不足或适配屏幕时自动翻转或吸附到图片内侧。
- “选择并拖动”（浏览模式）下，所有图层保持可见但锁定：鼠标不会命中、选择或移动任何图元；左拖始终移动整张图片，滚轮按10%缩放图片并在左上角显示倍率2秒，`Delete`不会删除图元。标注模式下用`Space+左拖`移动，普通滚轮调整当前工具参数，`Ctrl+滚轮`调整钉图透明度。
- HUD、工具栏和属性区使用圆角外观；窗口激活与失焦时使用不同描边、阴影和透明度，方便识别当前操作对象。
- 标注模式普通滚轮调整当前线宽、圆圈大小、字号或橡皮擦半径；只有进入相应标注编辑工具并选择已有标注后，滚轮才调整该对象的对应参数。各工具及四种序号子类型分别记忆参数和颜色。
- 文字工具会在底图原位建立一个字位宽的输入框并在图片边界内自动换行；点击图片空白区域或按`Enter`提交，`Esc`取消。文字框与字号同步缩放，可设置无背景、方角/圆角背景、背景颜色和透明度。圆圈序号可带从圆周切点连接目标的指针。
- 橡皮擦提供对象、笔刷和框选三种方式；右键菜单可切换模式、锁定、置顶、清空标注、复制、保存或适配屏幕。
- 带OCR结果的钉图支持“仅原图、不可见可选、可见叠加、仅OCR”四种文字层，可调整文字/背景透明度、颜色和字体，并选择普通文字或表格直接复制。
- “智能脱敏”会在本机识别人名、公司名称和红色印章，先列出候选、置信度和位置，只有勾选并确认后才添加遮盖。没有OCR结果时仍可单独检测红章。
- 脱敏支持实体遮盖、马赛克和虚化。遮盖是可撤销标注，原图始终不变；复制或保存时才真正写入输出图片像素。
- 被任一脱敏方式覆盖的OCR文字和表格值会同时从可选文字层、Unicode文本、TSV和HTML Table中移除，避免从剪贴板绕过遮挡。
- 支持撤销、重做和删除所选。复制、PNG/JPEG另存均输出处理后图片，默认不覆盖原图。
- `Esc`默认关闭钉图；正在输入文字时第一次`Esc`只取消输入。可在“设置 → 贴图”关闭Esc退出行为。
- 钉图标注、四种序号计数、窗口位置、缩放、透明度和OCR文字层设置会保存到本机工作区。正常关闭后按`F3`可恢复最近工作区；程序异常中断时，下次启动会恢复此前仍打开的钉图。

## 7. 历史与页签

- 历史记录和当前结果页签均保存在本机。
- 关闭窗口后可按设置恢复上次页签；恢复后可继续编辑、复核和复制。
- 历史保存数量、页签数量和是否恢复均可在“设置 → 常规与截图”调整。
- 删除历史会同时删除对应的本机缓存图片，请确认后操作。

## 8. 在线服务与隐私

- PP-OCRv6本地识别完全离线，不上传截图或识别文字。
- 在线OCR、翻译和AI校核只在用户主动点击时联网。
- API Key使用Windows当前账户加密保存。
- AI校核默认只发送文字；上传原图需要开启权限并逐次确认。

## 9. 检查更新

- 软件每天最多自动检查一次GitHub正式版本；检查失败不影响离线OCR。
- 可在托盘菜单“检查更新”或“设置 → 关于”手动检查。
- 发现新版本后，软件只提示并打开官方GitHub Release页面，不会静默安装。
- 下载后请核对Release中的SHA256；覆盖安装会保留当前用户的设置、授权、历史和截图。

官方发布仓库：  
<https://github.com/litongchao7212/offline-screenshot-ocr-releases>

## 10. 常见问题

### 截图窗口空白或无法创建结果页

先退出托盘程序后重新启动。如仍失败，打开“设置 → 隐私与诊断 → 组件与诊断”，运行自检并保留活动日志。

### Excel/WPS中的长编号或前导零发生变化

优先选择“保持文本”或“全部按文本”后重新复制。程序会同时写入TSV和带文本类型标记的HTML Table；仍应在粘贴后抽查关键编号。

### 无法打开Markdown使用说明

程序会优先使用Windows默认Markdown应用；没有关联应用时会用记事本打开。也可以直接打开安装目录中的`使用说明.md`。

### GitHub无法连接

更新检查不会影响本地功能。可稍后重试，或通过作者发布的可信渠道获取安装包并核对SHA256。

## 11. 授权与作者

- 首次使用提供30天本机试用。
- 正式授权通过机器码和离线授权码完成，不依赖联网验证。
- 作者：李同超
- 作者公众号：工程商务杂坛

© 2026 李同超，保留所有权利。未经许可不得复制、分发或用于商业用途；第三方组件遵循各自许可证。
