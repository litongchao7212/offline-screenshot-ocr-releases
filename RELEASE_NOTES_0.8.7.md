# Offline Screenshot OCR 0.8.7

0.8.7 聚焦截图贴图稳定性、跨屏体验、文字编辑和异常恢复，不改变现有离线 OCR 模型、授权与用户数据目录。

## 主要改进

- 支持逐显示器 DPI 定位，修复第二屏、负坐标显示器和缩放后工具栏偏移。
- 文字输入点击空白即可提交，Esc 取消；新建文字框从一个字位开始，字号与文字框同步缩放。
- 文字标注支持无背景、方角、圆角、背景颜色和透明度，并纳入撤销、重做与工具记忆。
- 钉图工作区可保存标注、四类独立序号、窗口位置、缩放、透明度、工具栏和 OCR 图层状态。
- 支持异常退出恢复、正常关闭后 F3 恢复、工作区损坏备份恢复和显示器断开后归位。
- 剪贴板写入改为异步队列，繁忙时不阻塞界面，释放后可恢复。
- 增加连续截图、资源增长、混合 DPI、启动预算和安装升级数据保护门禁。

## 验证结果

- 159 项 Python 回归通过，2 项按环境设计跳过。
- 连续截图等价压力、钉图渲染、工作区恢复和历史版本兼容门禁通过。
- 稳态句柄、GDI 和 USER 对象增长均为 0。
- 临时目录全新安装、同版本覆盖、运行时校验和卸载通过。
- 从 0.8.6 覆盖升级到 0.8.7，升级前 86 个用户文件逐一 SHA256 一致。
- 正式版首次启动后，设置、凭据、历史图片和会话等 79 个受保护文件哈希零变化。

## 平台边界

- 支持 Windows 10 1809 及以上版本、Windows 11 x64。
- 本轮自动验收环境为 Windows 11 24H2；100%、125%、150%、负坐标副屏已由坐标模型覆盖。
- 本轮没有真实 Windows 10 和真实混合 DPI 双屏硬件证据，因此不宣称对应人工验收通过。

## 文件校验

- 文件：`Offline Screenshot OCR-0.8.7-Setup.exe`
- GitHub 下载资产：`Offline.Screenshot.OCR-0.8.7-Setup.exe`（GitHub 会强制将 Release 资产名中的空格规范化为点号）
- 大小：`181,464,089` 字节
- SHA256：`c959aebbccb39116b719f1cbbb83c67bd318142e24ce6baf0d7cefefe001d708`
